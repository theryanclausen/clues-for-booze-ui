query queryQuestion($category:Int){
    question(category:$category){
        responseCode
        question{
            question
            correctAnswer
            incorrectAnswers
        }
    }
}